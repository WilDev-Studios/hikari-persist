# API Reference

Welcome to the `hikari-persist` API reference.

Most users should start here:
[`hikaripersist.cache`](cache.md#hikaripersist.cache.Cache)

This module provides the main cache interface for interacting with the persistent cache.